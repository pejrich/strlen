[package]
name = "strlen"
version = "0.1.0"
authors = []
edition = "2021"

[lib]
name = "strlen"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
rustler = "0.35.0"
unicode-segmentation = "1.10.1"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
